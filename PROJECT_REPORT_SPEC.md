# 「尋找你的『樹』適圈」平台技術規格與設計說明書

> 供期末報告撰寫使用的完整文字參考文件  
> 撰寫時間：2026-05-27  
> 課程：政治大學「社會傳播導向大數據分析」114-2 學期期末專案

---

## 壹、專案定位與核心問題意識

本平台以「台北市路樹倒塌的制度性失靈」為核心議題，採用**融媒體報導**的呈現形式，將深度報導與互動查詢工具整合於同一平台。查詢工具的設計目標是讓讀者在閱讀報導後，能立即查詢自身周遭路樹的健康風險狀態，將抽象的制度問題轉化為個人化、可感知的視覺體驗。

平台的問題意識建立在以下三項背景事實之上：

1. **褐根病是路樹倒塌的主要病因**，佔通報病害的約 57.5%，但患病樹木外觀難以辨識，一般市民無從察覺。
2. **台灣缺乏專業樹藝師制度**，全台僅約 90 位 ISA 國際認證樹藝師，攀樹師更僅有 4 位，人力嚴重不足。
3. **制度覆蓋不均**：22 縣市中只有 7 個建立褐根病防治措施，10 個設有例行巡檢 SOP，8 個要求人員具備專業資格。

---

## 貳、資料來源與蒐集過程

平台共整合四大類資料來源，分述如下。

### 一、台北市行道樹清查資料（主表）

**來源機關：** 台北市政府資料大平台  
**資料集連結：** `https://data.taipei/dataset/detail?id=7a49d00c-a5ff-4a6b-be9e-aaa6dc1ff7e8`  
**更新頻率：** 每月更新  
**蒐集方式：** 直接下載 CSV 檔案  

**資料規模：**
- 總筆數：92,927 筆（台北市全部列管行道樹）
- 欄位數：11 欄
- 涵蓋範圍：12 個行政區

**主要欄位：**

| 欄位名稱 | 說明 | 型態 |
|---|---|---|
| `tree_id` | 樹木唯一編號（如 BT0614021096） | 字串 |
| `dist` | 所在行政區 | 字串 |
| `region` | 所在道路名稱 | 字串 |
| `tree_type` | 樹種中文名稱 | 字串 |
| `diameter` | 胸徑（公分） | 數值 |
| `tree_height` | 樹高（公尺） | 數值 |
| `twd97x` / `twd97y` | TWD97 坐標（非通用經緯度，需轉換） | 數值 |

**座標系統說明：** 原始資料採用 **TWD97（EPSG:3826）** 坐標系統，此為台灣政府資料常見的本土坐標格式，無法直接用於 Leaflet、Google Maps 等通用地圖套件。資料整合過程中使用 R 語言的 `sf` 套件將所有坐標轉換為 **WGS84（EPSG:4326）** 的經緯度格式，並儲存為 `lng`、`lat` 欄位，轉換後的資料存為 `tree_integrated_wgs84.csv`。

---

### 二、台北市路樹健康狀態資料（geopkl）

**來源機關：** 台北市政府工務局公園路燈工程管理處（geopkl）  
**取得方式：** 透過 ArcGIS REST API 空間查詢  
**API 端點：**
```
GET https://arcgis.tpgos.gov.taipei/arcgis/rest/services/PKL/TreeBackEndService_107/MapServer/0/query
```
（Layer 0 為行道樹健康檢查資料）

**資料規模：** 84,438 筆成功配對（配對率 91%；松山區、信義區因 geopkl 覆蓋不完整，配對率相對偏低）

**主要欄位：**

| 欄位名稱 | 說明 | 範例值 |
|---|---|---|
| `health_樹木狀況` | 樹木狀況代碼 | P（正常）/ I（異常） |
| `health_健檢扣分` | 健康檢查扣分（負數，越低越危險，範圍約 0 至 -62） | -24 |
| `health_健檢關鍵因子` | 扣分主要原因文字描述 | 種植範圍土壤面積不足 |
| `health_風險結果` | 官方評定風險等級 | D級 / 低 / 中 / 重 / 高 |
| `health_樹冠頂梢枯死率` | 樹冠頂梢枯死比例（%） | 0 |
| `health_樹冠缺失率` | 樹冠缺失比例（%） | 13 |

**風險結果分布（全台北市）：**
- D 級（定期維護觀察，基本正常）：46,500 棵
- 無資料（NA，未配對或未健檢）：35,881 棵
- 空白（健檢資料待補）：9,942 棵
- 低風險：508 棵
- 中風險：42 棵
- 重風險：25 棵（需立即處理）
- 高風險：23 棵（需立即處理）

---

### 三、林業試驗所褐根病暨病害通報資料

**來源機關：** 林業試驗所「林木疫情監測系統」  
**取得方式：** 透過 POST API 以程式自動爬取  
**API 端點：**
```
POST https://healthapi.tfri.gov.tw/CaseReport/GroupByDamageLocation
```
**Request Body 範例：**
```json
{
  "county": "臺北市",
  "damageClassId": 1510,
  "startTime": "2019-01-01",
  "endTime": "2026-04-29"
}
```
（`damageClassId: 1510` 為褐根病分類代碼，亦可查詢其他病害類型）

**資料規模：** 5,107 筆（2019 年至 2026 年 4 月）

**欄位說明：**

| 欄位名稱 | 說明 |
|---|---|
| `lon` | 通報點經度（WGS84） |
| `lat` | 通報點緯度（WGS84） |
| `caseNumber` | 案件編號 |
| `date` | 通報日期 |
| `commonDamageName` | 病害名稱（如：褐根病、腐朽、不當鋪面） |
| `year` | 通報年份 |

**病害種類分布（前五名）：**
1. 褐根病：2,935 筆（57.5%）
2. 腐朽：1,017 筆
3. 不當鋪面：398 筆
4. 移植不當：139 筆
5. 靈芝根基腐病：112 筆

**褐根病年度趨勢：**
- 2019 年：388 件
- 2020 年：493 件
- 2021 年：538 件
- 2022 年：492 件
- 2023 年：574 件（最高峰）
- 2024 年：393 件
- 2025 年：91 件（截至 4 月）
- 2026 年：182 件（截至 4 月）

---

### 四、台北市列管受保護樹木資料

**來源機關：** 台北市政府  
**蒐集方式：** 下載原始 CSV（`台北市受保護樹木.csv`）  
**資料規模：** 共 3,799 棵受保護老樹

**整合處理：** 其中 805 棵老樹透過空間坐標配對，與行道樹主表中的對應樹木合併（在行道樹資料點上標記為 `is_protected: TRUE`，並附上保護編號、樹胸圍、管理單位等資訊）。另外 2,994 棵未能配對至行道樹主表的老樹，另外儲存為 `protected_standalone.csv`，在地圖上以獨立圖層顯示。

**主要欄位：**
- 樹木編號、樹種名稱、樹種學名
- 樹胸徑寬度（公尺）、樹胸圍長度（公尺）
- 詳細地址、地理位置名稱、管理單位
- WGS84 經緯度

---

### 五、台北市樹穴資料

**來源機關：** 台北市政府工務局公園路燈工程管理處  
**蒐集方式：** 下載政府開放資料（原始坐標為 TWD97）  
**資料規模：** 2,254 筆  
**處理方式：** 使用 R 語言 `sf` 套件將 TWD97 坐標轉換為 WGS84，儲存為 `tree_pits_wgs84.csv`  
**記錄類型：** 帶狀樹穴（多棵樹共享的連續種植帶）為主，另含面積與長度等幾何屬性

---

### 六、輔助分析資料（用於圖表，非地圖圖層）

| 資料集 | 來源 | 內容 |
|---|---|---|
| `institution.csv` | 監察院 113 年路樹管理調查報告 | 22 縣市制度完備度評比（普查制度、SOP、專業資格、褐根病防治） |
| `compensation.csv` | 同上 | 各縣市路樹國賠件數（108-112 年合計 112 件） |
| `budget.csv` | 台北市政府工務局公園路燈工程管理處預算書 | 113-114 年路樹相關預算對比 |
| `collapse_cause.csv` | 新聞彙整 | 路樹倒塌原因分類統計 |

---

## 參、資料整合方法

### 一、坐標轉換：TWD97 → WGS84

台北市政府提供的行道樹資料與樹穴資料採用 TWD97 二度分帶坐標（單位為公尺），必須先轉換為 WGS84 十進位經緯度，才能在 Leaflet.js 等通用地圖套件中使用。

**使用工具：** R 語言 `sf` 套件  
**轉換流程：**
1. 以 `st_as_sf()` 將 twd97x、twd97y 欄位建立空間物件，指定 CRS 為 EPSG:3826（TWD97）
2. 以 `st_transform()` 轉換至 EPSG:4326（WGS84）
3. 以 `st_coordinates()` 取出轉換後的經緯度，新增 `lng`、`lat` 欄位

---

### 二、健康資料空間配對（20 公尺緩衝）

行道樹主表與 geopkl 健康檢查資料**沒有共同的主鍵欄位**，只能透過空間距離進行配對。

**配對邏輯：**
- 對每一棵行道樹，在 geopkl 資料中找出距離最近的健檢記錄
- 若最近記錄的距離 **≤ 20 公尺**，則視為配對成功，將健檢欄位合併至主表
- 配對距離記錄於 `dist_to_health` 欄位（單位：公尺）
- 配對成功與否記錄於 `matched` 欄位（TRUE/FALSE）

**配對結果：**
- 成功配對：84,438 筆（91%）
- 未配對：8,489 筆（9%，主要集中於松山區、信義區）
- 未配對樹木在平台顯示為「無健檢資料（灰色）」，並在彈出卡片中標注說明

---

### 三、褐根病通報整合（空間關聯）

為使每棵行道樹的資料卡片能顯示「附近是否有褐根病通報紀錄」，進行以下整合：

**計算邏輯：**
- 對每一棵行道樹，統計 brownrot_taipei.csv 中距離 **50 公尺以內**的病害通報點數量
- 彙整為 `brownrot_count`（通報次數）、`brownrot_latest_date`（最近通報日期）、`brownrot_damage_types`（涉及病害類型）三個欄位
- 以上資料已預先計算並儲存至 `tree_integrated_wgs84.csv` 中，前端不需即時運算

---

###四、列管老樹整合（空間配對 + 獨立圖層分割）

**整合策略：**
1. 將 3,799 棵保護老樹與 92,927 棵行道樹進行空間配對
2. 若老樹坐標與某棵行道樹的坐標距離 ≤ 10 公尺，則判定為「同一棵樹」，將保護老樹的資訊合併至行道樹記錄中（標記 `is_protected: TRUE`）
3. 無法配對的 2,994 棵老樹，單獨儲存為 `protected_standalone.csv`，在地圖上以金色獨立圖層呈現

這樣的設計避免了同一棵老樹在地圖上出現兩個重疊的標記點。

---

### 五、最終整合主表規格（tree_integrated_wgs84.csv）

**總筆數：** 101,488 筆（92,927 棵行道樹 + 虛擬褐根病通報點補充）  
**檔案大小：** 約 16 MB  
**欄位總數：** 26 個

完整欄位清單：

| 欄位名稱 | 說明 |
|---|---|
| `tree_id` | 樹木唯一編號 |
| `dist` | 行政區 |
| `region` | 道路名稱 |
| `tree_type` | 樹種名稱 |
| `diameter` | 胸徑（公分） |
| `tree_height` | 樹高（公尺） |
| `matched` | 是否成功配對健康資料（TRUE/FALSE） |
| `dist_to_health` | 與最近健康資料的距離（公尺） |
| `health_樹木狀況` | P（正常）/ I（異常） |
| `health_健檢扣分` | 健檢扣分（負數） |
| `health_健檢關鍵因子` | 扣分原因文字描述 |
| `health_風險結果` | 官方風險等級（D級/低/中/重/高） |
| `health_樹冠頂梢枯死率` | 樹冠頂梢枯死比例（%） |
| `health_樹冠缺失率` | 樹冠缺失比例（%） |
| `lng` | WGS84 經度 |
| `lat` | WGS84 緯度 |
| `is_protected` | 是否為列管保護樹木（TRUE/FALSE） |
| `prot_id` | 保護樹木編號 |
| `prot_name` | 保護樹木名稱 |
| `prot_sci` | 學名 |
| `prot_diam_m` | 保護樹木胸徑（公尺） |
| `prot_circ_m` | 保護樹木胸圍（公尺） |
| `prot_loc` | 保護位置描述 |
| `prot_mgr` | 管理單位 |
| `brownrot_count` | 附近（50m 內）褐根病暨病害通報次數 |
| `brownrot_latest_date` | 最近一筆通報日期 |
| `brownrot_damage_types` | 涉及病害類型 |

---

## 肆、平台功能設計說明

平台以單頁 HTML 檔案（`index.html`）實作，採用純前端架構，不需要伺服器端運算，所有資料讀取與渲染皆在瀏覽器中完成。

### 技術堆疊

| 技術 | 版本 | 用途 |
|---|---|---|
| Leaflet.js | 1.9.4 | 2D 互動地圖主引擎 |
| MapLibre GL JS | 4.x | 3D 建築物圖層渲染 |
| PapaParse | 5.4.1 | 瀏覽器端 CSV 串流解析 |
| CartoDB Dark Matter | — | 深色地圖底圖（Tile Server） |
| OSRM API | — | 開源路線規劃引擎 |
| Nominatim API | — | OpenStreetMap 地址搜尋與地理編碼 |
| 中央氣象署（CWA）開放資料 API | — | 即時天氣與警特報 |
| Google Fonts（Orbitron、Share Tech Mono） | — | 科技感字型 |

---

### 功能一：行道樹風險地圖主圖層

**資料來源：** `tree_integrated_wgs84.csv`（101,488 筆）  
**渲染方式：** 以 PapaParse 串流解析 CSV，每筆資料轉換為 Leaflet 標記物件

**風險等級判定邏輯：**

系統依據 `health_風險結果` 欄位將每棵樹歸類為四個風險等級：

| 風險等級 | 對應欄位值 | 地圖顏色 | 視覺效果 |
|---|---|---|---|
| 高/重風險 | `高`、`重` | 紅色（#f85149） | 脈衝動畫圓點（持續閃爍） |
| 中風險 | `中` | 橘色（#e3b341） | 靜態圓點，半徑 4px |
| 低/D 級風險 | `低`、`D級`、`低風險`、`極低風險` | 綠色（#3fb950） | 靜態圓點，半徑 3px |
| 無健檢資料 | `NA`、空白、未配對 | 灰色（#6e7681） | 預設隱藏；可透過篩選面板「未健檢樹木」選項顯示 |

**高風險視覺設計細節：** 高/重風險樹木採用 CSS `@keyframes` 動畫實作的雙環脈衝效果（`pulseRing` 動畫），從核心向外擴散消失，強化「需立即注意」的警示感。

**統計面板（左上角）：**
地圖左上角即時顯示目前篩選範圍內的高風險、中風險、低風險、**未健檢**、總計數量，隨篩選條件同步更新。「未健檢」數量的呈現刻意強調制度黑洞——從未接受健檢的樹木並非「低風險」，而是「從未被看見」，對應報導中「資訊分散與治理落差」的核心論點。

---

### 功能二：樹木詳細資訊彈出卡片

點擊任一樹木標記，彈出包含以下資訊的卡片（`popup`）：

**基本資訊區：**
- 樹種名稱（若為榕樹、菩提樹、黑板樹、盾柱木、雀榕等高風險樹種，標題旁自動顯示「⚠ 高風險樹種」橘色警示）
- 行政區與道路位置
- 胸徑（公分）、樹高（公尺）
- 官方樹木狀況（P 正常 / I 異常）

**健康評估區：**
- 健檢扣分（以進度條視覺化呈現，扣分範圍 0 至 -62 分轉換為 0%–100% 的進度條）
- 健檢關鍵因子（文字描述）：若因子內容含有「修枝」「截」「創傷」「腐朽」「無法癒合」「不定芽」等截頂修剪相關關鍵字，文字自動轉為橘色，並在下方加入說明框「⚠️ 疑似修剪傷害：截頂式修剪產生的傷口易在 3–5 年後導致枝幹斷裂」，對應報導中陳楷鴻副執行長所指出的不當修剪問題。
- 樹冠頂梢枯死率（%）
- 樹冠缺失率（%）

**列管保護老樹區（若適用）：**
- 顯示金色星星標記 ⭐ 以及保護編號、胸徑（公尺）、管理單位

**褐根病通報警示區（若附近有通報紀錄）：**
- 橘紅色警示區塊，顯示通報次數、最近通報日期、涉及病害類型

**風險判定依據說明：**
- 依據該樹木的資料狀況自動生成說明文字，包括：扣分程度描述（嚴重/中度/輕微）、關鍵因子解釋、枯死率與缺失率的顏色標記說明

**標籤徽章（底部）：**
卡片底部彙整所有適用的標籤，包括：官方風險等級、「高風險樹種」、「疑似修剪傷害」、「列管老樹」、「褐根病通報」。使用者可一眼掌握該棵樹同時具備哪些風險因子。

---

### 功能三：篩選控制面板

**位置：** 地圖右上角可展開面板

**篩選維度：**

1. **風險等級篩選**（四個 checkbox）
   - 高/重風險（預設開啟）
   - 中風險（預設開啟）
   - 低/D 級風險（預設開啟）
   - **未健檢樹木**（預設關閉）：開啟後顯示全市約 8,489 棵從未接受官方健檢的路樹，以半透明灰色圓點呈現。此設計旨在讓使用者直觀感受制度覆蓋的盲區，而非將無資料等同於低風險。

2. **通報資料圖層開關**
   - 褐根病通報（預設開啟，紫色方塊標記）
   - 其他病害通報（預設關閉，灰色方塊標記）

3. **其他圖層開關**
   - 列管保護老樹（預設開啟，金色方塊標記）
   - 樹穴位置（預設關閉，藍色方塊標記）

4. **行政區下拉選單**（動態從資料生成 12 個行政區選項）

**篩選機制：** 所有標記物件在資料載入時即全數建立並存入 `allMarkers` 陣列，篩選時僅執行 `marker.addTo(map)` 或 `marker.remove()` 的切換，不重新建立標記，確保篩選操作的流暢性。

---

### 功能四：路線風險規劃

**啟動方式：** 點擊頂部導覽列的「路線規劃」按鈕，左側滑入路線側欄

**操作流程：**
1. 輸入出發地（支援手動輸入或使用目前位置）
2. 輸入目的地
3. 選擇交通方式（步行 / 開車 / 騎車）
4. 按下「查詢路線」按鈕

**地理編碼：** 使用 Nominatim（OpenStreetMap）API 進行地址搜尋，自動補足「台北市」關鍵字以提升本地搜尋精度，並以下拉候選清單呈現搜尋結果。

**路線計算：** 使用 OSRM（Open Source Routing Machine）免費公共 API 取得路線，支援步行（`foot`）、開車（`driving`）、騎車（`bike`）三種模式，每次查詢返回最多 3 條備選路線。

**沿途風險分析邏輯：**
- 以路線每段折點坐標為基礎，建立邊界框（bounding box）加速篩選
- 對邊界框內所有樹木標記，計算每棵樹距路線最近線段的精確距離
- **緩衝距離設定：40 公尺**（計算樹木倒塌可能影響範圍）
- 使用平面距離近似公式（以緯度修正經度縮放），避免球面三角計算的效能負擔

**分析結果呈現：**
- 高風險沿途樹木數量、中風險樹木數量
- 路線距離（公里）與預估時間（分鐘）
- 高風險樹木列表（樹種、所在位置）
- 路線安全等級評估（以高風險樹木數量判定）
- 路線模式下，非沿途樹木全數從地圖隱藏，僅保留沿途樹木並放大標記尺寸，強化聚焦感

---

### 功能五：即時天氣與氣象警報整合

**資料來源：** 中央氣象署（CWA）開放資料平台，使用 API 金鑰授權存取

**使用的 API 端點：**

| API 代碼 | 用途 |
|---|---|
| `O-A0001-001` | 臺北氣象站（站號 466920）即時觀測，取得溫度、濕度、風速、天氣狀況 |
| `O-A0003-001` | 全台北市自動站群，依行政區聚合氣象數據 |
| `W-C0033-001` | 台北市現行氣象警特報查詢（geocode 63） |

**天氣小卡（右上角）：**
顯示目前溫度（°C）、相對濕度（%）、天氣狀況、風速（m/s），並以 emoji 對應天氣類型（晴、陰、雨、雷雨等）。

**行政區切換：** 使用者切換篩選面板的行政區時，天氣小卡同步更新為該行政區的氣象站平均數據（多站平均）。

**氣象警報橫幅：** 符合下列任一條件時，頁面頂部顯示全寬度紅色警報橫幅（`#weather-alert`），並以閃爍動畫吸引注意：
- CWA W-C0033-001 API 返回台北市現行警特報
- 當前風速 ≥ 10.8 m/s（蒲福 6 級）或陣風 ≥ 15 m/s
- 天氣描述含「大雨」「豪雨」「暴雨」

**警報訊息設計：** 氣象警報的文字並非通用天氣提示，而是直接對應路樹倒塌的具體風險語言：
- 強風警報時顯示：「強風 X.X m/s，榕樹等淺根路樹倒塌風險升高」
- 豪大雨時顯示：「[天氣類型]，豪雨易使根部土層鬆動，請勿在大型路樹下方停留」

此設計對應報導中輿情高點所揭示的規律——颱風強風與豪雨是路樹倒塌的最主要氣象誘因（凱米颱風 2024/7、康芮颱風 2024/10），讓即時氣象資訊與核心報導議題直接連結，而非單純的天氣顯示功能。

---

### 功能六：使用者定位

**技術：** 瀏覽器原生 Geolocation API（`navigator.geolocation.getCurrentPosition`）

**兩種使用情境：**
1. 點擊地圖右下角定位按鈕（`◎`）：地圖飛到使用者位置，放置藍色脈衝定位標記
2. 路線規劃側欄的「使用目前位置」按鈕：定位後自動將坐標填入出發地欄位

**注意事項：** Geolocation API 在非 HTTPS 環境（如 http://）下，Chrome 等主流瀏覽器會拒絕授權。本機以 `localhost` 開發不受限制，正式部署需配置 HTTPS。

---

### 功能七：褐根病通報圖層

**資料：** `brownrot_taipei.csv`（5,107 筆，2019-2026 年）  
**標記顏色：**
- 褐根病：紫色（#bf5af2），正方形標記
- 其他病害：灰色（#6e7681），正方形標記

**預設狀態：** 褐根病通報點預設顯示；其他病害通報預設隱藏  
**彈出資訊：** 病害名稱、通報日期、案件編號

---

### 功能八：列管保護老樹獨立圖層

**資料：** `protected_standalone.csv`（2,994 棵，未能配對至行道樹主表的獨立老樹）  
**標記樣式：** 金色正方形（#f0a500），預設顯示  
**彈出資訊：** 樹種名稱（含學名）、地址、胸徑（公尺）、胸圍（公尺）、管理單位

---

### 功能九：3D 地圖模式

**引擎：** MapLibre GL JS，疊加 OpenStreetMap 建築物向量圖層  
**功能：** 可切換 2D 與 3D 視角，3D 模式下以建築物高度呈現城市立體感，更直觀地感受路樹與建築環境的空間關係  
**切換方式：** 頂部導覽列提供 2D/3D 模式切換按鈕

---

## 伍、UI/UX 設計理念

### 一、視覺風格定位：Cyberpunk HUD（賽博龐克抬頭顯示器）

平台採用**「科技感監控介面」（HUD, Heads-Up Display）** 作為整體視覺語言，刻意呼應報導議題中「制度疏漏難以被看見」的核心隱喻——透過前衛的視覺形式，讓原本隱形於日常環境的路樹風險「浮現」於使用者眼前。

**視覺特徵：**
- **深色底色**（`#020912`，接近純黑的深海藍）：減少視覺疲勞，強化資料點的視覺對比
- **科技藍（Cyan）主色**（`#00d4ff`）：用於標題、邊框發光效果、互動按鈕等，建立統一的色彩識別
- **霓虹發光邊框（Neon Glow Border）**：所有資訊面板（統計面板、篩選面板、圖例）使用 CSS `box-shadow` 製作青藍色發光邊框動畫，強化「儀表板介面」的視覺感
- **掃描線效果（Scanline Overlay）**：頁面初始載入時顯示全螢幕掃描線覆蓋層，載入完成後移除，製造「系統開機」的儀式感
- **Orbitron 字型**：標題採用 Google Fonts Orbitron 字型，此字型為幾何無襯線設計，常見於科幻影視的未來感介面，強化科技定調
- **Share Tech Mono 字型**：資料數值採用等寬字體，呼應「資料監控系統」的視覺語言

**顏色系統（CSS 變數）：**
```css
--cyan:       #00d4ff   /* 科技藍：主要強調色、邊框發光 */
--high:       #ff4757   /* 高風險紅 */
--mid:        #ffa502   /* 中風險橘 */
--low:        #2ed573   /* 低風險綠 */
--protected:  #ffd700   /* 保護老樹金 */
--brownrot:   #bf5af2   /* 褐根病紫 */
--bg-deep:    #020912   /* 深色背景 */
--bg-panel:   rgba(2,12,26,0.93)  /* 半透明面板底色 */
```

---

### 二、資訊層次設計

平台的介面設計依循「由概覽到細節」的資訊架構：

1. **地圖全覽層**：使用者首先看到全台北市路樹的風險分布，透過色彩立即掌握高風險聚集區域
2. **行政區篩選層**：透過下拉選單或統計面板，聚焦至特定行政區
3. **個別樹木層**：點擊特定標記，彈出該棵樹的完整健康資料與風險判定依據
4. **路線分析層**：輸入起終點，進一步聚焦至特定路徑沿途的風險分布

---

### 三、風險視覺化策略

**差異化視覺編碼原則：**
- 高風險樹木使用**動態效果**（脈衝動畫）與**較大尺寸**，在靜態地圖上自然凸顯
- 中風險採用暖橘色靜態圓點
- 低風險採用小尺寸綠色圓點，降低視覺噪音
- 無健檢資料的樹木完全隱藏，避免大量灰點造成干擾（資料仍保留於記憶體，供進階功能使用）

**顏色語意一致性：** 紅—橘—綠三色對應高—中—低風險，符合使用者對「交通燈號」的直覺認知，無需額外學習成本。

---

### 四、操作即時性設計

為維持地圖操作的流暢感，平台採用以下策略：

- **預載與快取策略**：所有圖層資料於頁面載入時一次性讀入記憶體，存於 JavaScript 陣列（`allMarkers`、`brownrotMarkers`、`protectedMarkers`）。篩選操作僅切換圖層顯示/隱藏，不重新讀取資料。
- **分層非同步載入**：行道樹主圖層（最大，~16MB）、褐根病圖層、保護老樹圖層分別獨立異步載入，主圖層完成後即移除載入遮罩，其他圖層在背景持續載入。
- **邊界框預篩選（BBox Culling）**：路線風險分析時，先以路線的最小邊界框快速排除遠距樹木，再對候選樹木執行精確距離計算，大幅減少不必要的運算量。

---

### 五、氣象警報的使用者安全考量

天氣資訊的整合並非純粹的資料展示，而是有實際安全意義的設計決策：強風、豪雨是路樹倒塌的重要誘發因素。氣象警報橫幅以全寬度紅色漸層背景搭配閃爍動畫，確保使用者在風險情境下能第一時間察覺警告，與報導議題形成直接呼應。

---

## 陸、R 語言統計圖表說明

本專案同時使用 R 語言製作用於報導頁面的靜態統計圖表，包含：

| 圖表 | 說明 |
|---|---|
| `fig1_district_risk.png` | 各行政區高風險路樹數量橫向長條圖 |
| `fig2_species.png` | 台北市前二十大樹種分布橫向長條圖 |
| `fig3_institution.png` | 全國 22 縣市制度完備度堆疊長條圖（普查/SOP/專業資格/褐根病防治） |
| `fig4_budget.png` | 台北市公園處 113-114 年路樹預算對比圖（含成長倍數標注） |
| `fig5_compensation.png` | 各縣市路樹國賠件數長條圖（前十名） |

---

## 柒、資料引用與版權說明

| 資料集 | 授權說明 | 引用要求 |
|---|---|---|
| 台北市行道樹資料 | 政府資料開放授權條款（CC 0 等同） | 可自由使用，建議標注來源 |
| geopkl 路樹健康資料 | 台北市政府工務局公園路燈工程管理處 | 標注「資料來源：台北市政府工務局公園路燈工程管理處」 |
| 林試所褐根病通報 | 林業試驗所林木疫情監測系統 | 引用需標注「林業試驗所」 |
| 監察院報告數據 | 政府公開報告 | 引用需標注「監察院 113 年路樹管理調查報告」 |
| 台北市公園處預算 | 台北市政府工務局公園路燈工程管理處 | 標注來源機關 |
| CWA 氣象資料 | 中央氣象署開放資料（CC BY 4.0） | 標注「資料來源：中央氣象署」 |
| OSM/CARTO 底圖 | © OpenStreetMap Contributors / © CARTO | 依授權顯示 attribution（已於地圖右下角顯示） |

---

## 捌、平台限制與說明

1. **健檢資料未涵蓋全部樹木：** 92,927 棵行道樹中有約 9% 因 geopkl 資料覆蓋不完整而無健檢資料，在地圖上以灰色標示「無健檢資料」，而非標示為低風險，避免誤導。

2. **褐根病通報點位不等於確診位置：** 通報資料記錄的是病害發現通報的地點，可能是附近路段的坐標，而非精確的單一棵樹位置，使用時應注意這一精度限制。

3. **本專案自建風險分析非官方標準：** 健檢扣分進度條、附近褐根病通報計數等輔助資訊為本專案自行設計的分析框架，與官方 `health_風險結果` 欄位的評估邏輯不同，不應引用為官方認定的風險數據。

4. **資料時效性：** 行道樹主表每月更新，健康檢查資料不定期更新，本平台使用截至 2026 年 4 月的靜態快照版本。

5. **資料量與效能：** CSV 總大小約 18MB，首次載入依網路條件可能需要 3-10 秒。本平台未使用地圖 tile server 或後端 API，完全依賴瀏覽器前端處理，在資料量擴大時可能需要改採後端架構。
